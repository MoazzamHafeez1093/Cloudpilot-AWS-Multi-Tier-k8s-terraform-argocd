apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
    project: cloudpilot
spec:
  type: ClusterIP          # internal only — not accessible from outside
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 4001
      targetPort: 4001